A very very simple Notepad like application made using Java.
Uses PrintWriter and Scanner class for basic IO operation.


Note -- The only flip side is that you need to provide a path as a String to an input dialog box when you are saving the file.
